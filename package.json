{
  "name": "@vr/root",
  "private": true,
  "dependencies": {
    "dotenv": "^16.3.1",
    "lerna": "^8.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "npx lerna run build"
  },
  "devDependencies": {
    "webpack-cli": "^5.1.4"
  }
}
